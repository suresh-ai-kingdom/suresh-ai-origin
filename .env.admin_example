"""
Quick Admin Setup - Add to .env for local testing
For production: Add to Render Environment Variables
"""

# Option 1: Simple (for local testing)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=SureshAI2026!

# Option 2: More Secure (recommended for production)
# Generate hash: python -c "import bcrypt; print(bcrypt.hashpw(b'your_password', bcrypt.gensalt()).decode())"
# Then use:
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD_HASH=$2b$12$your_generated_hash_here

# Instructions:
# 1. Copy these to your .env file (for local testing)
# 2. Add to Render Dashboard → Environment (for production)
# 3. Restart the app
# 4. Login at /admin/login with username: admin, password: SureshAI2026!
