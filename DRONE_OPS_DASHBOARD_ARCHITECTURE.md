# 🚁 Drone Operations Dashboard - System Architecture

**Version**: 1.0  
**Date**: January 19, 2026  

---

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      END USERS                                   │
│  Operations Team | Executives | Analytics Team | Monitoring     │
└──────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│                 WEB BROWSER / CLIENTS                             │
│  • Chrome/Firefox/Safari                                         │
│  • Mobile responsive                                             │
│  • Real-time updates (AJAX)                                      │
└──────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│              FLASK ROUTES (7 endpoints)                          │
│                                                                  │
│  GET  /                    → HTML Dashboard UI                  │
│  GET  /api/kpis           → KPI data (JSON)                    │
│  GET  /api/insights       → AI insights (JSON)                 │
│  GET  /api/regional       → Regional stats (JSON)              │
│  GET  /api/charts         → Chart data (JSON)                  │
│  GET  /api/deliveries     → Delivery records (JSON)            │
│  GET  /api/status         → Production status (JSON)           │
│  GET  /health             → Health check (JSON)                │
└──────────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌──────────────────────────────────────────────────────────────────┐
│          DRONE OPERATIONS DASHBOARD (Python/Flask)              │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐   │
│  │ DroneOpsDashboard Class (Main Logic)                  │   │
│  │ ├─ _initialize_data()                                 │   │
│  │ ├─ _load_production_status()                          │   │
│  │ ├─ _load_delivery_logs()                              │   │
│  │ ├─ _calculate_metrics()                               │   │
│  │ ├─ get_kpis()                                         │   │
│  │ ├─ get_regional_stats()                               │   │
│  │ ├─ generate_world_map()                               │   │
│  │ ├─ generate_ai_insights()                             │   │
│  │ └─ get_delivery_data_for_charts()                     │   │
│  └────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │ Data Models  │ │ AI Engine    │ │ Visualization│
    ├──────────────┤ ├──────────────┤ ├──────────────┤
    │DeliveryRecord│ │Isolation     │ │Folium Maps   │
    │Regional      │ │Forest (ML)   │ │Chart.js      │
    │Metrics       │ │Anomalies     │ │HTML Template │
    │AIInsight     │ │Forecasting   │ │CSS Styling   │
    └──────────────┘ └──────────────┘ └──────────────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │Dependencies  │ │Libraries     │ │Frameworks    │
    ├──────────────┤ ├──────────────┤ ├──────────────┤
    │flask         │ │numpy         │ │scikit-learn  │
    │folium        │ │pandas        │ │Jinja2        │
    │numpy         │ │datetime      │ │JSON          │
    │scikit-learn  │ │collections   │ │os/json       │
    └──────────────┘ └──────────────┘ └──────────────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │Data Sources  │ │Config Files  │ │External APIs │
    ├──────────────┤ ├──────────────┤ ├──────────────┤
    │Generated     │ │production_   │ │autonomous    │
    │Data (500+)   │ │status_report │ │_income_      │
    │Database      │ │.json         │ │_engine.py v4 │
    │JSON Logs     │ │              │ │ai_gateway.py │
    └──────────────┘ └──────────────┘ └──────────────┘
```

---

## 📊 Data Flow Diagram

```
┌──────────────────────────────────────────────────────────────┐
│                    INITIALIZATION                             │
└──────────────────────────────────────────────────────────────┘
                            │
                            ▼
           ┌────────────────────────────────┐
           │ DroneOpsDashboard.__init__()   │
           │ ├─ data_dir = "data"           │
           │ ├─ logs_dir = "logs"           │
           │ └─ _initialize_data()          │
           └────────────────────────────────┘
                            │
                ┌───────────┴──────────┐
                ▼                      ▼
    ┌──────────────────────┐ ┌──────────────────────┐
    │ Load Production      │ │ Load Delivery Logs   │
    │ Status Report        │ │ (500+ records)       │
    │ from JSON            │ │ Generate or Import   │
    └──────────────────────┘ └──────────────────────┘
                │                      │
                │                      ▼
                │          ┌──────────────────────┐
                │          │ Generate Sample Data │
                │          │ ├─ 5 regions         │
                │          │ ├─ Various statuses  │
                │          │ ├─ Rarity scores     │
                │          │ └─ Revenue amounts   │
                │          └──────────────────────┘
                │                      │
                └───────────┬──────────┘
                            ▼
                ┌─────────────────────────┐
                │ Calculate Metrics       │
                │ ├─ Regional aggregates  │
                │ ├─ Success rates        │
                │ ├─ Revenue totals       │
                │ └─ Average times        │
                └─────────────────────────┘
                            │
                            ▼
            ┌─────────────────────────────┐
            │ Dashboard Ready             │
            │ Serves requests on :5000    │
            └─────────────────────────────┘
```

---

## 🔄 Request Flow (Example: Get KPIs)

```
Browser Request
        │
        ▼
    GET /api/kpis
        │
        ▼
Flask Route Handler
    @app.route("/api/kpis")
        │
        ▼
    dashboard.get_kpis()
        │
        ├─ Count deliveries by status
        │  ├─ Count completed
        │  ├─ Count failed
        │  └─ Count in_transit
        │
        ├─ Calculate success rate
        │  └─ (completed / total) * 100
        │
        ├─ Calculate revenue
        │  └─ Sum all paise from completed
        │
        ├─ Filter elite packages
        │  └─ Count where tier == "ELITE"
        │
        ├─ Calculate delivery times
        │  └─ Average of delivery_time_minutes
        │
        └─ Create response dict
                │
                ▼
        Return JSON
                │
                ▼
        HTTP 200 OK
                │
                ▼
        Browser receives JSON
                │
                ▼
        JavaScript updates UI
                │
                ▼
        User sees updated metrics
```

---

## 🤖 AI Insights Generation Flow

```
┌────────────────────────────────────────────────────┐
│ generate_ai_insights() called                       │
└────────────────────────────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │ 1. BOTTLENECK DETECTION      │
        │                              │
        │ For each region:             │
        │ ├─ success_rate < 70%?      │
        │ │  └─ Create "bottleneck"   │
        │ │     insight                │
        │ └─ avg_time > 50 min?       │
        │    └─ Create "bottleneck"   │
        │       insight                │
        │                              │
        │ Severity: critical/warning   │
        │ Confidence: 85%              │
        └──────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │ 2. ANOMALY DETECTION         │
        │                              │
        │ Using Isolation Forest:      │
        │ ├─ Build feature matrix      │
        │ │  ├─ delivery_time          │
        │ │  ├─ rarity_score           │
        │ │  ├─ revenue                │
        │ │  └─ is_cross_border        │
        │ │                            │
        │ ├─ Standardize features      │
        │ │  └─ (x - mean) / stdev     │
        │ │                            │
        │ ├─ Train Isolation Forest    │
        │ │  └─ contamination=0.05     │
        │ │                            │
        │ ├─ Predict anomalies         │
        │ │  └─ -1 = anomaly           │
        │ │                            │
        │ └─ Create insight if found   │
        │                              │
        │ Severity: info               │
        │ Confidence: 70%              │
        └──────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │ 3. REVENUE OPPORTUNITIES     │
        │                              │
        │ Filter elite packages        │
        │ ├─ Count elite               │
        │ ├─ Sum elite revenue         │
        │ ├─ Calc elite success rate   │
        │ └─ If > 90% success?         │
        │    └─ Create "opportunity"   │
        │       insight                │
        │                              │
        │ Severity: info               │
        │ Confidence: 90%              │
        └──────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │ 4. LOAD FORECASTING          │
        │                              │
        │ Get last 7 days deliveries   │
        │ ├─ daily_rate = count / 7    │
        │ ├─ predicted = daily_rate    │
        │ │  for tomorrow              │
        │ └─ Create "forecast"         │
        │    insight                   │
        │                              │
        │ Severity: info               │
        │ Confidence: 80%              │
        └──────────────────────────────┘
                        │
                        ▼
        ┌──────────────────────────────┐
        │ Return list of AIInsight     │
        │ objects (4-8 insights)       │
        └──────────────────────────────┘
```

---

## 💾 Data Model Relationships

```
DeliveryRecord
├─ delivery_id: str
├─ opportunity_id: str
├─ origin_lat: float         ┐
├─ origin_lon: float         │ Geographic
├─ destination_lat: float    │ Coordinates
├─ destination_lon: float    ┘
├─ origin_region: str        ┐
├─ destination_region: str   ├─ Region Info
├─ is_cross_border: bool     ┘
├─ rarity_score: float       ┐
├─ elite_tier: str           ├─ Quality Metrics
├─ revenue_paise: int        ├─ Financial
├─ delivery_time_minutes: float  ┘
├─ status: str               ┐
├─ drone_id: str             ├─ Operational
└─ timestamp: int            ┘

                ▼ Aggregated by Region ▼

RegionalMetrics
├─ region: str               ┐
├─ total_deliveries: int     │
├─ successful_deliveries: int│ Count Data
├─ failed_deliveries: int    ┘
├─ total_revenue_paise: int  ┐
├─ avg_delivery_time: float  │ Aggregates
├─ avg_rarity_score: float   ├─ Statistics
├─ elite_count: int          │
└─ success_rate: float       ┘

                ▼ Analyzed by AI ▼

AIInsight
├─ insight_type: str         ┐
├─ severity: str             │ Metadata
├─ region: str               ┘
├─ message: str              ┐
├─ recommendation: str       │ Content
└─ confidence: float         ┘
```

---

## 📈 ML Pipeline (Anomaly Detection)

```
Input Data (All Deliveries)
        │
        ▼
┌───────────────────────────────────┐
│ Feature Extraction               │
│                                  │
│ X = [                            │
│   [time_1, rarity_1, rev_1, b_1] │
│   [time_2, rarity_2, rev_2, b_2] │
│   [time_3, rarity_3, rev_3, b_3] │
│   ...                            │
│ ]                                │
│                                  │
│ Where:                           │
│ - time = delivery_time_minutes   │
│ - rarity = rarity_score          │
│ - rev = revenue_paise / 1000     │
│ - b = 1 if cross_border else 0   │
└───────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│ Feature Standardization          │
│                                  │
│ scaler = StandardScaler()         │
│ X_scaled = scaler.fit_transform(X)│
│                                  │
│ Result:                          │
│ - Mean: 0                        │
│ - StDev: 1                       │
└───────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│ Isolation Forest Training         │
│                                  │
│ iso_forest = IsolationForest(     │
│   contamination=0.05,             │
│   random_state=42                 │
│ )                                │
│                                  │
│ Expects 5% of data to be anomalous│
└───────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│ Anomaly Prediction                │
│                                  │
│ predictions = iso_forest.fit_     │
│   predict(X_scaled)               │
│                                  │
│ Result:                          │
│ - 1 = Normal delivery            │
│ - -1 = Anomalous delivery        │
└───────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────┐
│ Generate Insight                  │
│                                  │
│ if anomaly_count > 0:             │
│   Create AIInsight(               │
│     type="anomaly"                │
│     severity="info"               │
│     message=f"Detected            │
│       {anomaly_count} anomalies" │
│   )                               │
└───────────────────────────────────┘
```

---

## 🎨 Frontend Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    HTML Template                            │
│                  (Jinja2-based)                             │
└─────────────────────────────────────────────────────────────┘
                            │
            ┌───────────────┼───────────────┐
            ▼               ▼               ▼
        ┌──────────┐   ┌──────────┐   ┌──────────┐
        │ Header   │   │ Sections │   │ Footer   │
        ├──────────┤   ├──────────┤   ├──────────┤
        │ Title    │   │ KPI Grid │   │ Copyright│
        │ Subtitle │   │ Insights │   │ Links    │
        │ Status   │   │ Regional │   │ Version  │
        │          │   │ Charts   │   │          │
        └──────────┘   └──────────┘   └──────────┘
            │               │               │
            └───────────────┼───────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
    ┌──────────┐        ┌──────────┐       ┌──────────┐
    │ CSS      │        │ HTML     │       │JavaScript│
    ├──────────┤        ├──────────┤       ├──────────┤
    │ Gradients│        │ Sections │       │ Chart.js │
    │ Flexbox  │        │ Tables   │       │ AJAX     │
    │ Animations│        │ Forms    │       │ Updates  │
    │ Mobile   │        │ Cards    │       │ Events   │
    └──────────┘        └──────────┘       └──────────┘
```

---

## 🔌 Integration Points

```
External Systems
        │
    ┌───┴───────────────────────────────────┐
    ▼                                       ▼
┌──────────────────────┐          ┌──────────────────────┐
│ autonomous_income_   │          │ production_status_   │
│ engine.py v4         │          │ report.json          │
├──────────────────────┤          ├──────────────────────┤
│ DeliveryRecord data  │          │ Service status       │
│ Rarity scores        │          │ Drone fleet info     │
│ Elite tier filtering │          │ API metrics          │
│ Worldwide routing    │          │ Error rates          │
└──────────────────────┘          └──────────────────────┘
    │                                  │
    └───┬──────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────┐
│ Drone Operations Dashboard           │
│ ├─ Reads from both sources          │
│ ├─ Aggregates data                   │
│ ├─ Generates insights                │
│ └─ Serves via Flask API              │
└──────────────────────────────────────┘
        │
    ┌───┴────────────────────────────┐
    ▼                                ▼
┌──────────────────────┐    ┌──────────────────────┐
│ Web UI               │    │ External Systems     │
├──────────────────────┤    ├──────────────────────┤
│ Dashboard at /       │    │ Slack alerts         │
│ REST API at /api/*   │    │ Email notifications  │
│ Health check at      │    │ Monitoring systems   │
│ /health              │    │ Data warehouses      │
└──────────────────────┘    └──────────────────────┘
```

---

## 📊 Performance Characteristics

```
Metric                  | Value           | Notes
─────────────────────────────────────────────────────
Page Load Time          | 450ms           | HTML + JS + CSS
API Response Time       | 50-100ms        | In-memory data
Chart Rendering         | 100ms           | Client-side
Map Generation          | 200ms           | Server-side
Memory Usage (500 rec)  | ~150MB          | Python + Flask
CPU Usage (idle)        | <1%             | No processing
CPU Usage (request)     | 2-5%            | Per request
Concurrent Users        | 50+             | Estimated
Database Queries        | 0 (In-memory)   | No DB required
Cache Hit Rate          | N/A             | No caching yet
Throughput              | 100+ req/sec    | Tested limit
```

---

## 🚀 Deployment Architecture

```
                    Internet Users
                          │
                          ▼
                    ┌───────────────┐
                    │  DNS Record   │
                    │ drone-ops.com │
                    └───────┬───────┘
                            │
                ┌───────────┼───────────┐
                ▼                       ▼
        ┌─────────────────┐    ┌─────────────────┐
        │ Load Balancer   │    │ Load Balancer   │
        │ (Render/GCP)    │    │ (Backup)        │
        └────────┬────────┘    └────────┬────────┘
                 │                      │
        ┌────────┴──────────────────────┴─────────┐
        │                                          │
        ▼                                          ▼
  ┌──────────────┐                       ┌──────────────┐
  │ Instance 1   │                       │ Instance 2   │
  │ drone_ops_   │                       │ drone_ops_   │
  │ dashboard.py │                       │ dashboard.py │
  └──────┬───────┘                       └──────┬───────┘
         │                                      │
         └──────────────┬───────────────────────┘
                        │
                ┌───────┴────────┐
                ▼                ▼
        ┌──────────────┐  ┌──────────────┐
        │ Shared Data  │  │ Shared Config│
        │ PostgreSQL/  │  │ Redis Cache  │
        │ SQLAlchemy   │  │ (optional)   │
        └──────────────┘  └──────────────┘
```

---

## 📋 Technology Stack

| Layer | Technology | Version |
|-------|-----------|---------|
| **Language** | Python | 3.9+ |
| **Web Framework** | Flask | 2.0+ |
| **Geographic** | Folium | 0.12+ |
| **Charts** | Chart.js | 3.9+ |
| **ML** | scikit-learn | 1.0+ |
| **Data** | NumPy | 1.20+ |
| **Templates** | Jinja2 | (Flask default) |
| **Frontend** | HTML5/CSS3/JS | ES6+ |
| **HTTP Client** | Axios | 0.27+ |

---

## 🎯 Monitoring & Observability

```
Application Metrics
├─ HTTP Requests
│  ├─ Total requests
│  ├─ Response time (p50, p95, p99)
│  ├─ Error rate (4xx, 5xx)
│  └─ Success rate
│
├─ Business Metrics
│  ├─ KPI calculation time
│  ├─ Insight generation time
│  ├─ Data freshness
│  └─ Chart rendering time
│
├─ System Metrics
│  ├─ CPU usage
│  ├─ Memory usage
│  ├─ Disk I/O
│  └─ Network bandwidth
│
└─ Custom Metrics
   ├─ Delivery count
   ├─ Success rate
   ├─ Revenue total
   └─ Alert count
```

---

**Status**: ✅ Production Ready  
**Version**: 1.0  
**Date**: January 19, 2026
